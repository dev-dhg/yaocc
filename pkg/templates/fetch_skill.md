---
name: fetch
description: Fetch the content of a URL (HTTP GET).
---

To fetch the content of a specific URL, use the `fetch` command.

```bash
yaocc fetch "<url>"
```

**Arguments:**
- `<url>`: The URL to fetch.

**Example:**
```bash
yaocc fetch "https://example.com"
```
